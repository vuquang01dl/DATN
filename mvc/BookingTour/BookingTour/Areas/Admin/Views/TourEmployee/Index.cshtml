@model Presentation.Areas.Admin.Models.TourEmployeeViewModel
@{
    ViewData["Title"] = "Danh sách khách hàng trong Tour";
}

<div class="page-header">
    <h3 class="fw-bold mb-3">Danh sách khách hàng Chuyến Đi</h3>
    <ul class="breadcrumbs mb-3">
        <li class="nav-home">
            <a asp-area="Admin"
               asp-controller="Home"
               asp-action="Index">
                <i class="icon-home"></i>
            </a>
        </li>
        <li class="separator">
            <i class="icon-arrow-right"></i>
        </li>
        <li class="nav-home">
            <a asp-area="Admin"
               asp-controller="Tour"
               asp-action="Index">
                Tours
            </a>
        </li>
        <li class="separator">
            <i class="icon-arrow-right"></i>
        </li>
        <li class="nav-home active">
            @Model.Tour.Title
        </li>
        <li class="separator">
            <i class="icon-arrow-right"></i>
        </li>
        <li class="nav-home active">
            Employees
        </li>
    </ul>
</div>


<div class="row">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h4 class="card-title">Danh sách Filters</h4>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="multi-filter-select"
                           class="display table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>STT</th>
                                <th>Image</th>
                                <th>Employee Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Position</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>STT</th>
                                <th>Image</th>
                                <th>Employee Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Position</th>
                            </tr>
                        </tfoot>
                        <tbody>
                            @for (int i = 0; i < Model.Employees.Count; i++)
                            {
                                <tr>
                                    <td>@(i + 1)</td>
                                    <td><img src="~/assetPrivate/img/employee/@Model.Employees[i].Image" alt="@Model.Employees[i].Image" style="width:150px;height:100px; object-fit:cover" /></td>
                                    <td>@Model.Employees[i].FullName</td>
                                    <td>@Model.Employees[i].Email</td>
                                    <td>@Model.Employees[i].Phone</td>
                                    <td>@Model.Employees[i].Position</td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <div class="d-flex align-items-center">
                    <h4 class="card-title">Thông tin địa điểm tour: @Model.Tour.Title</h4>
                    <a class="btn btn-primary btn-round ms-auto"
                       asp-area="Admin"
                       asp-action="Create"
                       asp-route-TourID="@Model.TourID"
                       asp-controller="TourEmployee">
                        <i class="fa fa-plus"></i>
                        Thêm nhân viên
                    </a>
                </div>
            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table id="add-row"
                           class="display table table-striped table-hover">
                        <thead>
                            <tr>
                                <th>STT</th>
                                <th>Image</th>
                                <th>Employee Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Position</th>
                                <th style="width: 10%">Action</th>
                            </tr>
                        </thead>
                        <tfoot>
                            <tr>
                                <th>STT</th>
                                <th>Image</th>
                                <th>Employee Name</th>
                                <th>Email</th>
                                <th>Phone</th>
                                <th>Position</th>
                                <th>Action</th>
                            </tr>
                        </tfoot>
                        <tbody>
                            @for (int i = 0; i < Model.Employees.Count; i++)
                            {
                                <tr>
                                    <td>@(i + 1)</td>
                                    <td><img src="~/assetPrivate/img/employee/@Model.Employees[i].Image" alt="@Model.Employees[i].Image" style="width:150px;height:100px; object-fit:cover" /></td>
                                    <td>@Model.Employees[i].FullName</td>
                                    <td>@Model.Employees[i].Email</td>
                                    <td>@Model.Employees[i].Phone</td>
                                    <td>@Model.Employees[i].Position</td>
                                    <td>
                                        <div class="form-button-action">
                                            <a class="btn btn-link btn-danger " type="button"
                                               asp-area="Admin"
                                               asp-route-TourID="@Model.TourID"
                                               asp-route-EmployeeID="@Model.Employees[i].EmployeeID"
                                               asp-controller="TourEmployee"
                                               asp-action="Delete">
                                                <i class="fa fa-times"></i>
                                            </a>

                                        </div>

                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

